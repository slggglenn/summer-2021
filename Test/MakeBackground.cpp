#include "MakeBackground.h"

void initializeBackground(VertexArray &VArefs, IntRect background) {
    /*VertexArray background;
    background.setPrimitiveType(Quads);
    background.resize(10 * 10 * 4);*/

   // const unsigned int TILE_SIZE = 32;
   // const unsigned int TILE_TYPES = 
    // set coords for each vertex

    // load texure
}